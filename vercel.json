{
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider"
  },
  "installCommand": "pnpm approve-builds && pnpm install",
  "buildCommand": "pnpm install --ignore-scripts=false && pnpm run build"
}
